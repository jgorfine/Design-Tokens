import { FontData, GoogleWebFontData } from '@diez/prefabs';
/**
 * Encapsulates logic to handle multiple Google Fonts variants under the same
 * font family.
 */
export declare class GoogleFont {
    private name;
    private variants;
    constructor(name: string, variants?: GoogleWebFontData[]);
    static isGoogleFont(font: FontData): boolean;
    private getHashedName;
    private getHashedVariations;
    hash(): string;
    addVariant({ style, weight }: GoogleWebFontData): void;
}
/**
 * Encapsulates logic to manage a collection of [[GoogleFont]]s methods to fetch
 * the fonts from the Google Fonts servers.
 */
export declare class GoogleFontCollection {
    private baseUrl;
    private collections;
    set(fontName: string, data: GoogleWebFontData): void;
    get url(): string;
}
//# sourceMappingURL=google-fonts.d.ts.map